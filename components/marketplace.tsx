'use client'

import { useMemo, useState } from 'react'
import { Check, MapPin, X } from 'lucide-react'
import { useLanguage } from '@/lib/language'

export type MarketplaceView = 'marketplace' | 'offers'
type Listing = { id: string; crop: string; icon: string; farmer: string; location: string; quantity: number; price: number; date: string; quality: string }
type OfferStatus = 'Pending' | 'Accepted' | 'Rejected'
export type Offer = { id: string; buyer: string; crop: string; quantity: number; price: number; expected: number; location: string; status: OfferStatus }

const listings: Listing[] = [
  { id: 'ramesh-onion', crop: 'Onion', icon: '🧅', farmer: 'Ramesh Patil', location: 'Nashik, Maharashtra', quantity: 20, price: 2650, date: '5 Sept 2026', quality: 'Grade A' },
  { id: 'onion-35', crop: 'Onion', icon: '🧅', farmer: 'Suresh Jadhav', location: 'Nashik', quantity: 35, price: 2600, date: '6 Sept 2026', quality: 'Grade A' },
  { id: 'tomato-15', crop: 'Tomato', icon: '🍅', farmer: 'Meena Shinde', location: 'Pune', quantity: 15, price: 2200, date: '5 Sept 2026', quality: 'Grade A' },
  { id: 'soybean-30', crop: 'Soybean', icon: '🌱', farmer: 'Vilas Pawar', location: 'Ahilyanagar', quantity: 30, price: 4700, date: '7 Sept 2026', quality: 'Grade A' },
]
const buyers = [{ name: 'ABC Foods', location: 'Nashik', crops: 'Onion, Tomato', quantity: '10–100 quintals' }, { name: 'FreshMart', location: 'Pune', crops: 'Onion, Potato', quantity: '20–80 quintals' }, { name: 'AgroTrade', location: 'Nashik', crops: 'Onion, Soybean', quantity: '15–60 quintals' }]
const money = (value: number) => `₹${Math.round(value).toLocaleString('en-IN')}`

function Badge({ children, tone = 'muted' }: { children: React.ReactNode; tone?: 'muted' | 'green' | 'amber' | 'red' }) { const colors = { muted: 'bg-muted text-muted-foreground', green: 'bg-emerald-100 text-emerald-800', amber: 'bg-amber-100 text-amber-900', red: 'bg-red-100 text-red-800' }; return <span className={`inline-flex rounded-full px-2.5 py-1 text-[10px] font-bold uppercase tracking-wide ${colors[tone]}`}>{children}</span> }

function OfferModal({ listing, close, submit }: { listing: Listing; close: () => void; submit: (offer: Offer) => void }) { const [price, setPrice] = useState(2700); const [quantity, setQuantity] = useState(listing.quantity); const [message, setMessage] = useState('Interested in purchasing the full quantity.'); const [error, setError] = useState(''); const onSubmit = () => { if (price <= 0) return setError('Offer price must be greater than 0.'); if (quantity <= 0) return setError('Quantity must be greater than 0.'); if (quantity > listing.quantity) return setError(`Offer quantity cannot exceed ${listing.quantity} quintals.`); submit({ id: `offer-${Date.now()}`, buyer: 'ABC Foods', crop: listing.crop, quantity, price, expected: listing.price, location: listing.location, status: 'Pending' }) }; return <div className="fixed inset-0 z-50 flex items-end justify-center bg-foreground/30 p-0 sm:items-center sm:p-4" role="dialog" aria-modal="true"><div className="w-full max-w-lg rounded-t-3xl bg-card p-6 shadow-2xl sm:rounded-3xl"><div className="flex items-start justify-between"><div><Badge>Prototype Offer</Badge><h2 className="mt-3 font-serif text-2xl font-bold">Make an offer</h2></div><button onClick={close} aria-label="Close offer dialog" className="rounded-full p-2 hover:bg-muted"><X className="size-5" /></button></div><div className="mt-5 grid grid-cols-2 gap-4 rounded-2xl bg-muted p-4 text-sm"><div><p className="text-xs text-muted-foreground">Produce</p><p className="mt-1 font-bold">{listing.icon} {listing.crop}</p></div><div><p className="text-xs text-muted-foreground">Available</p><p className="mt-1 font-bold">{listing.quantity} quintals</p></div><div><p className="text-xs text-muted-foreground">Farmer expected</p><p className="mt-1 font-bold">{money(listing.price)}/q</p></div></div><div className="mt-5 space-y-4"><label className="block text-sm font-semibold">Offer price<input className="mt-2 h-12 w-full rounded-xl border border-input bg-background px-3" type="number" min="1" value={price} onChange={e => setPrice(Number(e.target.value))} /></label><label className="block text-sm font-semibold">Quantity (quintals)<input className="mt-2 h-12 w-full rounded-xl border border-input bg-background px-3" type="number" min="1" max={listing.quantity} value={quantity} onChange={e => setQuantity(Number(e.target.value))} /></label><label className="block text-sm font-semibold">Message<textarea className="mt-2 min-h-20 w-full rounded-xl border border-input bg-background p-3" value={message} onChange={e => setMessage(e.target.value)} /></label>{error && <p className="rounded-xl bg-red-50 p-3 text-sm font-semibold text-red-700">{error}</p>}<button onClick={onSubmit} className="min-h-12 w-full rounded-xl bg-primary px-5 font-bold text-primary-foreground">Submit Offer</button></div></div></div> }

function ListingCard({ listing, open }: { listing: Listing; open: (listing: Listing) => void }) { return <article className="rounded-2xl border border-border bg-card p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className="text-3xl">{listing.icon}</p><h3 className="mt-2 font-serif text-xl font-bold">{listing.crop}</h3><p className="mt-1 text-sm text-muted-foreground">{listing.farmer}</p></div><Badge tone="green">{listing.quality}</Badge></div><div className="mt-5 grid grid-cols-2 gap-4 border-y border-border py-4 text-sm"><div><p className="text-xs text-muted-foreground">Location</p><p className="mt-1 font-semibold">{listing.location}</p></div><div><p className="text-xs text-muted-foreground">Quantity</p><p className="mt-1 font-semibold">{listing.quantity} quintals</p></div><div><p className="text-xs text-muted-foreground">Expected price</p><p className="mt-1 font-bold text-primary">{money(listing.price)}/q</p></div><div><p className="text-xs text-muted-foreground">Available</p><p className="mt-1 font-semibold">{listing.date}</p></div></div><div className="mt-4 flex gap-3"><button onClick={() => open(listing)} className="min-h-11 flex-1 rounded-xl bg-accent px-3 text-sm font-bold text-accent-foreground">Make Offer</button><button onClick={() => open(listing)} className="min-h-11 rounded-xl border border-border px-3 text-sm font-bold text-primary">View Listing</button></div></article> }

function BuyerCards() { return <section className="space-y-4"><div><p className="text-sm font-semibold text-primary">Prototype buyers</p><h2 className="mt-1 font-serif text-2xl font-bold">Trusted by transparency</h2></div><div className="grid gap-4 md:grid-cols-3">{buyers.map(buyer => <article key={buyer.name} className="rounded-2xl border border-border bg-card p-5"><div className="flex items-start justify-between"><h3 className="font-serif text-lg font-bold">{buyer.name}</h3><Badge>Prototype Buyer</Badge></div><p className="mt-3 flex items-center gap-2 text-sm text-muted-foreground"><MapPin className="size-4 text-primary" />{buyer.location}</p><p className="mt-4 text-xs text-muted-foreground">Interested in</p><p className="mt-1 text-sm font-semibold">{buyer.crops}</p><p className="mt-3 text-xs text-muted-foreground">Typical quantity</p><p className="mt-1 text-sm font-semibold">{buyer.quantity}</p></article>)}</div></section> }

export function BuyerMarketplace({ openOffers, context }: { openOffers: () => void; context: { crop: string; quantity: number; location: string; market: string; marketPrice: number; netReturn: number } }) { const { t } = useLanguage(); const [cropFilter, setCropFilter] = useState('All crops'); const [locationFilter, setLocationFilter] = useState('All locations'); const [minPrice, setMinPrice] = useState(''); const [selected, setSelected] = useState<Listing | null>(null); const [submitted, setSubmitted] = useState<Offer | null>(null); const filtered = useMemo(() => listings.filter(item => (cropFilter === 'All crops' || item.crop === cropFilter) && (locationFilter === 'All locations' || item.location === locationFilter) && (!minPrice || item.price >= Number(minPrice))), [cropFilter, locationFilter, minPrice]); return <div className="space-y-8"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-sm font-semibold text-primary">{t.buyerMarketplaceTag}</p><h1 className="mt-2 font-serif text-3xl font-bold md:text-4xl">{t.findBuyers}</h1><p className="mt-2 text-muted-foreground">{t.findBuyersSubtitle}</p></div><button onClick={openOffers} className="min-h-11 rounded-xl border border-primary px-4 text-sm font-bold text-primary">{t.myOffers}</button></div><section className="rounded-2xl border border-border bg-card p-4 shadow-sm"><div className="grid gap-3 sm:grid-cols-3"><label className="text-xs font-bold text-muted-foreground">{t.filterCrop}<select value={cropFilter} onChange={e => setCropFilter(e.target.value)} className="mt-2 h-11 w-full rounded-xl border border-input bg-background px-3 text-sm text-foreground"><option>All crops</option><option>Onion</option><option>Tomato</option><option>Soybean</option></select></label><label className="text-xs font-bold text-muted-foreground">{t.filterLocation}<select value={locationFilter} onChange={e => setLocationFilter(e.target.value)} className="mt-2 h-11 w-full rounded-xl border border-input bg-background px-3 text-sm text-foreground"><option>All locations</option><option>Nashik</option><option>Pune</option><option>Ahilyanagar</option></select></label><label className="text-xs font-bold text-muted-foreground">{t.minPrice}<input value={minPrice} onChange={e => setMinPrice(e.target.value)} placeholder="₹ / quintal" type="number" className="mt-2 h-11 w-full rounded-xl border border-input bg-background px-3 text-sm text-foreground" /></label></div></section>{submitted && <div className="rounded-2xl border border-emerald-200 bg-emerald-50 p-5 text-emerald-900"><div className="flex items-start gap-3"><Check className="mt-0.5 size-5" /><div><h2 className="font-serif text-xl font-bold">{t.offerSubmittedTitle}</h2><p className="mt-1 text-sm">{submitted.buyer} · {money(submitted.price)}/q · {submitted.quantity} quintals</p><p className="mt-2 text-sm font-semibold">{t.pendingFarmerResponse}</p></div></div></div>}<div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{filtered.map(listing => <ListingCard key={listing.id} listing={listing} open={setSelected} />)}</div><BuyerCards /><section className="rounded-2xl bg-muted p-5 text-sm text-muted-foreground"><p className="font-bold text-foreground">Transparent offers</p><p className="mt-1">You can always see the buyer name, offer price, quantity, location, and status. All listings and buyers are sample data for this prototype.</p></section>{selected && <OfferModal listing={selected} close={() => setSelected(null)} submit={offer => { setSubmitted(offer); setSelected(null); openOffers() }} />} </div> }

export function FarmerOffers({ offers, setOffers, openMarketplace, context }: { offers: Offer[]; setOffers: React.Dispatch<React.SetStateAction<Offer[]>>; openMarketplace: () => void; context: { crop: string; quantity: number; location: string; market: string; marketPrice: number; netReturn: number } }) { const { t } = useLanguage(); const defaultOffer: Offer = { id: 'demo-offer', buyer: 'ABC Foods', crop: context.crop, quantity: context.quantity, price: 2700, expected: context.marketPrice, location: context.location, status: 'Pending' }; const shown = offers.length ? offers : [defaultOffer]; const update = (id: string, status: OfferStatus) => setOffers(current => (current.length ? current.map(item => item.id === id ? { ...item, status } : item) : [{ ...defaultOffer, id, status }])); return <div className="space-y-7"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-sm font-semibold text-primary">{t.farmerDecisionsTag}</p><h1 className="mt-2 font-serif text-3xl font-bold">{t.buyerOffersTitle}</h1><p className="mt-2 text-muted-foreground">{t.buyerOffersSubtitle}</p></div><button onClick={openMarketplace} className="min-h-11 rounded-xl bg-accent px-4 text-sm font-bold text-accent-foreground">{t.findMoreBuyers}</button></div><section className="rounded-2xl border border-border bg-card p-5 shadow-sm"><p className="text-sm font-bold">{t.currentContextTitle}</p><div className="mt-4 grid grid-cols-2 gap-4 text-sm md:grid-cols-5"><Info label="Crop" value={context.crop} /><Info label="Quantity" value={`${context.quantity} quintals`} /><Info label="Best market" value={context.market} /><Info label="Market price" value={`${money(context.marketPrice)}/q`} /><Info label="Expected net return" value={money(context.netReturn)} /></div></section><div className="grid gap-5 lg:grid-cols-2">{shown.map(offer => <OfferCard key={offer.id} offer={offer} accept={() => update(offer.id, 'Accepted')} reject={() => update(offer.id, 'Rejected')} />)}</div><section className="rounded-2xl border border-border bg-card p-5"><h2 className="font-serif text-xl font-bold">{t.offerComparisonTitle}</h2><div className="mt-4 grid gap-3 sm:grid-cols-3">{[{ buyer: 'ABC Foods', price: 2700, quantity: context.quantity }, { buyer: 'FreshMart', price: 2650, quantity: context.quantity }, { buyer: 'AgroTrade', price: 2750, quantity: 15 }].map((item, index) => <div key={item.buyer} className={`rounded-xl p-4 ${index === 0 ? 'bg-accent/20 ring-1 ring-accent' : 'bg-muted'}`}><div className="flex justify-between gap-2"><p className="font-bold">{item.buyer}</p>{index === 0 && <Badge>Best offer</Badge>}</div><p className="mt-3 font-serif text-xl font-bold text-primary">{money(item.price)}/q</p><p className="mt-1 text-sm text-muted-foreground">{item.quantity} q · {money(item.price * item.quantity)} total</p></div>)}</div><p className="mt-4 text-xs text-muted-foreground">Price is only one factor. Consider quantity, location, buyer information, and status before deciding.</p></section></div> }

function OfferCard({ offer, accept, reject }: { offer: Offer; accept: () => void; reject: () => void }) { const tone = offer.status === 'Accepted' ? 'green' : offer.status === 'Rejected' ? 'red' : 'amber'; const diff = offer.price - offer.expected; return <article className="rounded-2xl border border-border bg-card p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><div><h2 className="font-serif text-xl font-bold">{offer.buyer}</h2><p className="mt-1 text-sm text-muted-foreground">Prototype Buyer · {offer.location}</p></div><Badge tone={tone}>{offer.status}</Badge></div><div className="mt-5 grid grid-cols-2 gap-4 border-y border-border py-4 text-sm"><Info label="Crop" value={offer.crop} /><Info label="Quantity" value={`${offer.quantity} quintals`} /><Info label="Offer" value={`${money(offer.price)}/q`} /><Info label="Expected" value={`${money(offer.expected)}/q`} /><Info label="Difference" value={`${diff >= 0 ? '+' : '-'}${money(Math.abs(diff))}/q`} /><Info label="Total offer value" value={money(offer.price * offer.quantity)} /></div>{offer.status === 'Accepted' && <div className="mt-4 rounded-xl bg-emerald-50 p-4 text-sm text-emerald-900"><p className="font-bold">Offer Accepted</p><p className="mt-1">Buyer connection established in prototype. Actual payment and logistics are outside this prototype.</p></div>}{offer.status === 'Rejected' && <p className="mt-4 text-sm font-semibold text-red-700">Offer rejected. You can continue viewing other offers.</p>}{offer.status === 'Pending' && <div className="mt-4 flex gap-3"><button onClick={accept} className="min-h-11 flex-1 rounded-xl bg-primary px-4 text-sm font-bold text-primary-foreground">Accept</button><button onClick={reject} className="min-h-11 flex-1 rounded-xl border border-destructive px-4 text-sm font-bold text-destructive">Reject</button></div>}</article> }
function Info({ label, value }: { label: string; value: string }) { return <div><p className="text-xs text-muted-foreground">{label}</p><p className="mt-1 font-bold">{value}</p></div> }
export function useMarketplaceOffers() { return useState<Offer[]>([]) }
export { listings }